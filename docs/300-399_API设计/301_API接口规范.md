# API接口规范

**版本**: v2.1.0  
**更新日期**: 2025-08-24  
**基于**: 系统架构设计_v3.0.0.md  
**适用范围**: 产品/设计/前后端/测试  
**编写人**: 后端开发师  

## 版本修订记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| v2.1.0 | 2025-08-24 | 统一版本号格式为三段版本号，添加版本修订记录 | 系统整理 |
| v2.1 | 2025-08-22 | 完善认证授权机制，增加WebSocket接口规范 | 后端开发师 |
| v2.0 | 2025-08-20 | 重新设计API架构，统一响应格式 | 后端开发师 |
| v1.0 | 2025-08-17 | 初始版本，定义基础API接口规范 | 后端开发师 |  

## 1. 接口设计原则

### 1.1 RESTful设计规范

**资源导向** - 以资源为中心设计API，使用名词而非动词  
**HTTP方法语义** - 正确使用GET、POST、PUT、DELETE等方法  
**状态码规范** - 使用标准HTTP状态码表示请求结果  
**版本控制** - 通过URL路径进行版本管理  

### 1.2 统一响应格式

```typescript
// 标准响应格式
interface APIResponse<T = any> {
  success: boolean;
  data?: T;
  error?: {
    code: string;
    message: string;
    details?: any;
  };
  timestamp: string;
  traceId: string;
}

// 分页响应格式
interface PaginatedResponse<T> extends APIResponse<T[]> {
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
    hasMore: boolean;
  };
}

// 成功响应示例
{
  "success": true,
  "data": { "id": "123", "name": "示例数据" },
  "timestamp": "2025-08-22T10:00:00Z",
  "traceId": "abc123def456"
}

// 错误响应示例
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数验证失败",
    "details": {
      "field": "email",
      "reason": "邮箱格式不正确"
    }
  },
  "timestamp": "2025-08-22T10:00:00Z",
  "traceId": "abc123def456"
}
```

## 2. 认证与授权

### 2.1 JWT Token认证

```typescript
// 请求头格式
Authorization: Bearer <JWT_TOKEN>

// JWT Payload结构
interface JWTPayload {
  userId: string;
  username: string;
  email: string;
  role: 'admin' | 'manager' | 'operator' | 'viewer';
  companyId: string;
  permissions: string[];
  iat: number;  // 签发时间
  exp: number;  // 过期时间
}
```

### 2.2 认证相关接口

#### POST /api/v1/auth/login
用户登录获取访问令牌

**请求体**:
```json
{
  "username": "user@example.com",
  "password": "password123"
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "dGhpcyBpcyBhIHJlZnJlc2g...",
    "expiresIn": 3600,
    "user": {
      "id": "123",
      "username": "user@example.com",
      "role": "operator",
      "companyId": "456"
    }
  }
}
```

#### POST /api/v1/auth/refresh
刷新访问令牌

**请求体**:
```json
{
  "refreshToken": "dGhpcyBpcyBhIHJlZnJlc2g..."
}
```

#### POST /api/v1/auth/logout
用户登出

**请求头**: Authorization: Bearer <token>

## 3. 对话相关接口

### 3.1 会话管理

#### GET /api/v1/chat/sessions
获取用户的对话会话列表

**查询参数**:
- `page`: 页码（默认1）
- `limit`: 每页数量（默认20）
- `status`: 会话状态过滤

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "session123",
      "title": "产品视频制作讨论",
      "status": "active",
      "messageCount": 15,
      "lastMessageAt": "2025-08-22T09:30:00Z",
      "createdAt": "2025-08-22T08:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 5,
    "totalPages": 1,
    "hasMore": false
  }
}
```

#### POST /api/v1/chat/sessions
创建新的对话会话

**请求体**:
```json
{
  "title": "新产品介绍视频制作"
}
```

#### GET /api/v1/chat/sessions/:sessionId
获取指定会话详情

**响应**:
```json
{
  "success": true,
  "data": {
    "id": "session123",
    "title": "产品视频制作讨论",
    "status": "active",
    "createdAt": "2025-08-22T08:00:00Z",
    "messages": [
      {
        "id": "msg001",
        "role": "user",
        "content": "我想制作一个新产品介绍视频",
        "aiId": null,
        "createdAt": "2025-08-22T08:05:00Z"
      },
      {
        "id": "msg002",
        "role": "ai",
        "content": "我来帮您制作产品介绍视频。请告诉我产品的主要特点...",
        "aiId": "ai_123",
        "createdAt": "2025-08-22T08:05:30Z"
      }
    ],
    "cards": [
      {
        "id": "card001",
        "messageId": "msg002",
        "type": "collaboration",
        "title": "产品信息收集",
        "data": {
          "tasks": [
            "上传产品图片",
            "提供产品描述",
            "确认目标受众"
          ]
        },
        "status": "active"
      }
    ]
  }
}
```

### 3.2 消息操作

#### POST /api/v1/chat/sessions/:sessionId/messages
发送消息到指定会话

**请求体**:
```json
{
  "content": "我的产品是智能手表，主要特点是健康监测和运动追踪",
  "type": "text"
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "userMessage": {
      "id": "msg003",
      "role": "user",
      "content": "我的产品是智能手表...",
      "createdAt": "2025-08-22T08:10:00Z"
    },
    "aiResponse": {
      "id": "msg004",
      "role": "ai",
      "content": "很好！智能手表是很有市场前景的产品...",
      "aiId": "ai_124",
      "createdAt": "2025-08-22T08:10:15Z"
    },
    "generatedCards": [
      {
        "id": "card002",
        "type": "video",
        "title": "智能手表介绍视频脚本",
        "data": {
          "script": "开场：展示手表外观...",
          "duration": "60秒",
          "scenes": 5
        }
      }
    ]
  }
}
```

#### GET /api/v1/chat/messages/:messageId/cards
获取消息关联的卡片

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "card001",
      "type": "video",
      "title": "产品介绍视频",
      "status": "active",
      "data": { /* 卡片具体数据 */ }
    }
  ]
}
```

## 4. 素材管理接口

### 4.1 文件夹管理

#### GET /api/v1/materials/folders
获取文件夹树结构

**查询参数**:
- `expand`: 是否展开所有层级（默认false）

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "folder_001",
      "name": "产品图片",
      "parentId": null,
      "path": "/产品图片",
      "children": [
        {
          "id": "folder_002",
          "name": "手表系列",
          "parentId": "folder_001",
          "path": "/产品图片/手表系列",
          "children": [],
          "fileCount": 12
        }
      ],
      "fileCount": 45,
      "createdAt": "2025-08-20T10:00:00Z"
    }
  ]
}
```

#### POST /api/v1/materials/folders
创建新文件夹

**请求体**:
```json
{
  "name": "新品发布",
  "parentId": "folder_001"
}
```

#### PUT /api/v1/materials/folders/:folderId
更新文件夹信息

**请求体**:
```json
{
  "name": "更新后的文件夹名称"
}
```

#### DELETE /api/v1/materials/folders/:folderId
删除文件夹

**查询参数**:
- `force`: 是否强制删除（包含文件的文件夹，默认false）

### 4.2 文件管理

#### GET /api/v1/materials/files
获取文件列表

**查询参数**:
- `folderId`: 文件夹ID
- `type`: 文件类型过滤（image/video/audio）
- `search`: 搜索关键词
- `page`: 页码
- `limit`: 每页数量

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "file_001",
      "name": "产品展示图.jpg",
      "type": "image",
      "size": 2048576,
      "url": "https://cdn.example.com/files/file_001.jpg",
      "thumbnailUrl": "https://cdn.example.com/thumbnails/file_001_thumb.jpg",
      "folderId": "folder_002",
      "metadata": {
        "width": 1920,
        "height": 1080,
        "format": "JPEG"
      },
      "createdAt": "2025-08-21T14:30:00Z"
    }
  ],
  "pagination": { /* 分页信息 */ }
}
```

#### POST /api/v1/materials/upload
上传文件

**请求**: multipart/form-data
- `file`: 文件数据
- `folderId`: 目标文件夹ID
- `name`: 文件名（可选，默认使用原文件名）

**响应**:
```json
{
  "success": true,
  "data": {
    "id": "file_002",
    "name": "新上传文件.mp4",
    "type": "video",
    "size": 10485760,
    "url": "https://cdn.example.com/files/file_002.mp4",
    "uploadProgress": 100,
    "createdAt": "2025-08-22T10:15:00Z"
  }
}
```

#### DELETE /api/v1/materials/files/:fileId
删除文件

#### POST /api/v1/materials/files/:fileId/move
移动文件到其他文件夹

**请求体**:
```json
{
  "targetFolderId": "folder_003"
}
```

### 4.3 搜索接口

#### GET /api/v1/materials/search
搜索素材

**查询参数**:
- `q`: 搜索关键词
- `type`: 文件类型过滤
- `folderId`: 限制搜索范围的文件夹ID
- `page`: 页码
- `limit`: 每页数量

## 5. 视频制作接口

### 5.1 脚本管理

#### GET /api/v1/videos/scripts
获取视频脚本列表

**查询参数**:
- `type`: 脚本类型（trending/custom/template）
- `search`: 搜索关键词
- `tags`: 标签过滤

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "script_001",
      "title": "产品介绍模板",
      "type": "template",
      "content": {
        "scenes": [
          {
            "id": 1,
            "description": "产品外观展示",
            "duration": 10,
            "shots": ["特写", "全景"]
          }
        ],
        "totalDuration": 60,
        "musicSuggestion": "轻快背景音乐"
      },
      "tags": ["产品介绍", "科技"],
      "performanceScore": 8.5,
      "usageCount": 25,
      "createdAt": "2025-08-20T09:00:00Z"
    }
  ]
}
```

#### POST /api/v1/videos/scripts
保存新脚本

**请求体**:
```json
{
  "title": "智能手表产品脚本",
  "type": "custom",
  "content": {
    "scenes": [/* 场景数据 */],
    "totalDuration": 90
  },
  "tags": ["智能手表", "健康"],
  "sourceUrl": "https://example.com/original-video"
}
```

### 5.2 视频合成

#### POST /api/v1/videos/jobs
创建视频合成任务

**请求体**:
```json
{
  "scriptId": "script_001",
  "materialIds": ["file_001", "file_002", "file_003"],
  "settings": {
    "resolution": "1920x1080",
    "format": "mp4",
    "quality": "high"
  },
  "jobName": "智能手表产品介绍视频"
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "jobId": "job_20250822_001",
    "status": "pending",
    "progress": 0,
    "estimatedDuration": 300,
    "createdAt": "2025-08-22T10:20:00Z"
  }
}
```

#### GET /api/v1/videos/jobs/:jobId
获取合成任务状态

**响应**:
```json
{
  "success": true,
  "data": {
    "jobId": "job_20250822_001",
    "status": "processing",
    "progress": 45,
    "currentStage": "video_rendering",
    "estimatedTimeRemaining": 180,
    "resultUrl": null,
    "errorMessage": null,
    "logs": [
      {
        "timestamp": "2025-08-22T10:21:00Z",
        "level": "info",
        "message": "开始处理素材..."
      }
    ]
  }
}
```

#### PUT /api/v1/videos/jobs/:jobId/cancel
取消合成任务

#### GET /api/v1/videos/jobs
获取用户的视频任务列表

**查询参数**:
- `status`: 任务状态过滤
- `page`: 页码
- `limit`: 每页数量

## 6. 矩阵管理接口

### 6.1 社媒账号管理

#### GET /api/v1/matrix/accounts
获取社媒账号列表

**响应**:
```json
{
  "success": true,
  "data": [
    {
      "id": "account_001",
      "platform": "douyin",
      "accountName": "智能科技展示",
      "accountId": "dy_123456789",
      "status": "active",
      "lastSyncAt": "2025-08-22T09:00:00Z",
      "followerCount": 15000,
      "authExpiresAt": "2025-09-22T10:00:00Z"
    }
  ]
}
```

#### POST /api/v1/matrix/accounts
添加新的社媒账号

**请求体**:
```json
{
  "platform": "xiaohongshu",
  "authCode": "auth_code_from_platform"
}
```

#### PUT /api/v1/matrix/accounts/:accountId/refresh
刷新账号授权

#### DELETE /api/v1/matrix/accounts/:accountId
删除社媒账号

### 6.2 内容发布

#### POST /api/v1/matrix/publish
发布内容到社媒平台

**请求体**:
```json
{
  "videoUrl": "https://cdn.example.com/videos/video_001.mp4",
  "title": "智能手表新品发布",
  "description": "最新智能手表，健康监测新体验...",
  "tags": ["智能手表", "科技", "健康"],
  "accountIds": ["account_001", "account_002"],
  "publishAt": "2025-08-22T18:00:00Z",
  "settings": {
    "allowComment": true,
    "allowShare": true,
    "visibility": "public"
  }
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "publishId": "publish_001",
    "status": "scheduled",
    "results": [
      {
        "accountId": "account_001",
        "platform": "douyin",
        "status": "pending",
        "publishAt": "2025-08-22T18:00:00Z"
      }
    ]
  }
}
```

#### GET /api/v1/matrix/publish/:publishId
获取发布任务状态

#### GET /api/v1/matrix/analytics
获取发布数据分析

**查询参数**:
- `startDate`: 开始日期
- `endDate`: 结束日期
- `platform`: 平台过滤
- `accountId`: 账号过滤

## 7. 错误码规范

### 7.1 HTTP状态码

| 状态码 | 说明 | 使用场景 |
|--------|------|----------|
| 200 | 成功 | 请求成功处理 |
| 201 | 已创建 | 资源创建成功 |
| 400 | 请求错误 | 参数验证失败 |
| 401 | 未授权 | Token无效或过期 |
| 403 | 禁止访问 | 权限不足 |
| 404 | 未找到 | 资源不存在 |
| 409 | 冲突 | 资源冲突（如重复创建） |
| 429 | 请求过多 | 触发限流 |
| 500 | 服务器错误 | 系统内部错误 |

### 7.2 业务错误码

```typescript
// 错误码定义
enum ErrorCode {
  // 认证相关 (1000-1099)
  INVALID_CREDENTIALS = 'AUTH_1001',
  TOKEN_EXPIRED = 'AUTH_1002',
  INSUFFICIENT_PERMISSIONS = 'AUTH_1003',
  
  // 用户相关 (1100-1199)
  USER_NOT_FOUND = 'USER_1101',
  USER_ALREADY_EXISTS = 'USER_1102',
  
  // 会话相关 (1200-1299)
  SESSION_NOT_FOUND = 'CHAT_1201',
  SESSION_EXPIRED = 'CHAT_1202',
  
  // 文件相关 (1300-1399)
  FILE_NOT_FOUND = 'FILE_1301',
  FILE_TOO_LARGE = 'FILE_1302',
  INVALID_FILE_TYPE = 'FILE_1303',
  UPLOAD_FAILED = 'FILE_1304',
  
  // 视频相关 (1400-1499)
  JOB_NOT_FOUND = 'VIDEO_1401',
  JOB_ALREADY_CANCELLED = 'VIDEO_1402',
  SCRIPT_NOT_FOUND = 'VIDEO_1403',
  COMPOSITION_FAILED = 'VIDEO_1404',
  
  // 矩阵相关 (1500-1599)
  ACCOUNT_NOT_FOUND = 'MATRIX_1501',
  AUTH_EXPIRED = 'MATRIX_1502',
  PUBLISH_FAILED = 'MATRIX_1503',
  PLATFORM_ERROR = 'MATRIX_1504',
  
  // 系统相关 (9000-9999)
  INTERNAL_ERROR = 'SYS_9001',
  SERVICE_UNAVAILABLE = 'SYS_9002',
  RATE_LIMIT_EXCEEDED = 'SYS_9003'
}
```

### 7.3 错误响应示例

```json
{
  "success": false,
  "error": {
    "code": "FILE_1302",
    "message": "上传文件过大",
    "details": {
      "maxSize": "100MB",
      "actualSize": "150MB",
      "field": "file"
    }
  },
  "timestamp": "2025-08-22T10:30:00Z",
  "traceId": "abc123def456"
}
```

## 8. 接口安全

### 8.1 API签名机制

```typescript
// 签名计算方法
function generateSignature(
  method: string,
  path: string,
  timestamp: string,
  body: string,
  secret: string
): string {
  const message = `${method}|${path}|${timestamp}|${body}`;
  return crypto
    .createHmac('sha256', secret)
    .update(message)
    .digest('hex');
}

// 请求头示例
{
  "Authorization": "Bearer <jwt_token>",
  "X-Timestamp": "1692691200",
  "X-Signature": "a1b2c3d4e5f6...",
  "Content-Type": "application/json"
}
```

### 8.2 限流策略

| 端点类型 | 限制 | 窗口期 |
|----------|------|--------|
| 登录接口 | 5次/IP | 15分钟 |
| 对话接口 | 60次/用户 | 1分钟 |
| 上传接口 | 10次/用户 | 1分钟 |
| 搜索接口 | 100次/用户 | 1分钟 |
| 发布接口 | 20次/用户 | 1小时 |

---

**文档维护**: 后端开发团队  
**审核状态**: 待审核  
**相关文档**: 系统架构设计, 数据库设计文档