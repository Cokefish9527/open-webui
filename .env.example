# Ollama URL for the backend to connect
# The path '/ollama' will be redirected to the specified backend URL
OLLAMA_BASE_URL='http://localhost:11434'

OPENAI_API_BASE_URL=''
OPENAI_API_KEY=''

# AUTOMATIC1111_BASE_URL="http://localhost:7860"

# For production, you should only need one host as
# fastapi serves the svelte-kit built frontend and backend from the same host and port.
# To test with CORS locally, you can set something like
# CORS_ALLOW_ORIGIN='http://localhost:5173;http://localhost:8080'
CORS_ALLOW_ORIGIN='*'

# For production you should set this to match the proxy configuration (127.0.0.1)
FORWARDED_ALLOW_IPS='*'

# DO NOT TRACK
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false

####################################
# 存储配置 (Storage Configuration)
####################################

# STORAGE_PROVIDER: 存储提供者类型
# 可选值: "local" (本地存储), "s3" (S3兼容存储，如阿里云OSS), "gcs" (Google Cloud Storage), "azure" (Azure Blob Storage)
# 默认值: "local"
STORAGE_PROVIDER=local

# UPLOAD_DIR: 本地存储目录路径
# 当STORAGE_PROVIDER为"local"时使用此配置
# 默认值: "./uploads"
UPLOAD_DIR=./uploads

####################################
# 阿里云OSS (S3兼容) 配置
####################################

# S3_ACCESS_KEY_ID: 阿里云访问密钥ID
# 用于身份验证的访问密钥ID，可在阿里云控制台获取
S3_ACCESS_KEY_ID=

# S3_SECRET_ACCESS_KEY: 阿里云访问密钥
# 与访问密钥ID配对使用的密钥，用于身份验证
S3_SECRET_ACCESS_KEY=

# S3_REGION_NAME: 阿里云OSS区域名称
# 指定OSS存储桶所在的区域，如"oss-cn-hangzhou"
S3_REGION_NAME=

# S3_BUCKET_NAME: 阿里云OSS存储桶名称
# 指定要使用的OSS存储桶名称
S3_BUCKET_NAME=

# S3_ENDPOINT_URL: 阿里云OSS服务端点URL
# 指定OSS服务的访问端点，格式为"https://oss-{region}.aliyuncs.com"
S3_ENDPOINT_URL=

# S3_KEY_PREFIX: OSS对象键前缀
# 用于在存储桶中组织文件的前缀，类似于文件夹路径
S3_KEY_PREFIX=

# S3_USE_ACCELERATE_ENDPOINT: 是否使用传输加速端点
# 启用后可提高跨地域传输性能，但可能会产生额外费用
# 可选值: True/False
# 默认值: False
S3_USE_ACCELERATE_ENDPOINT=False

# S3_ADDRESSING_STYLE: S3寻址风格
# 可选值: "virtual" (虚拟寻址), "path" (路径寻址)
# 虚拟寻址是推荐方式，但某些旧版本客户端可能需要使用路径寻址
S3_ADDRESSING_STYLE=virtual

# S3_ENABLE_TAGGING: 是否启用对象标签
# 启用后可为上传的文件添加标签，便于管理和检索
# 可选值: True/False
# 默认值: False
S3_ENABLE_TAGGING=False

####################################
# Google Cloud Storage 配置
####################################

# GCS_BUCKET_NAME: Google Cloud Storage存储桶名称
GCS_BUCKET_NAME=

# GOOGLE_APPLICATION_CREDENTIALS_JSON: Google服务账户凭证JSON
GOOGLE_APPLICATION_CREDENTIALS_JSON=

####################################
# Azure Blob Storage 配置
####################################

# AZURE_STORAGE_ENDPOINT: Azure存储账户端点
AZURE_STORAGE_ENDPOINT=

# AZURE_STORAGE_CONTAINER_NAME: Azure存储容器名称
AZURE_STORAGE_CONTAINER_NAME=

# AZURE_STORAGE_KEY: Azure存储账户密钥
AZURE_STORAGE_KEY=

####################################
# Playwright MCP 自动化配置
####################################

# PLAYWRIGHT_MCP_ENDPOINT: MCP Runner 地址 (例如 http://playwright-mcp-runner:8080)
PLAYWRIGHT_MCP_ENDPOINT=

# PLAYWRIGHT_CREDENTIAL_ROOT: 账号凭证 JSON 文件目录
PLAYWRIGHT_CREDENTIAL_ROOT=

# SOCIAL_VPN_PROXY_DIR: 账号代理/VPN 配置目录
SOCIAL_VPN_PROXY_DIR=

# PLAYWRIGHT_ARTIFACT_DIR: 截图、HAR 等执行产物输出目录
PLAYWRIGHT_ARTIFACT_DIR=playwright-mcp-artifacts

# PLAYWRIGHT_HEADLESS: 是否启用无头模式 (true/false)
PLAYWRIGHT_HEADLESS=true

####################################
# Chrome DevTools 工作区映射配置
####################################

# CHROME_DEVTOOLS_WORKSPACE_ROOT: Chrome DevTools 需要映射的工作区根目录
# 默认情况下返回当前项目路径，也可以根据需要自定义
# CHROME_DEVTOOLS_WORKSPACE_ROOT=D:/Work/hsch/open-webui

# CHROME_DEVTOOLS_WORKSPACE_UUID: 工作区的可选固定 UUID，用于在多个会话间保持一致
# 如果未配置，将在运行时自动生成一次性 UUID
# CHROME_DEVTOOLS_WORKSPACE_UUID=00000000-0000-4000-8000-000000000000
